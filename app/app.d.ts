// Declare systemJs __moduleName global variable. This allows us to assign ModuleId on components so that we dont need to specify paths to the templateUrl (makes moving the component/template around easier) 
declare var __moduleName : string;
